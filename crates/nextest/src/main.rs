fn main() {
    compile_error!(
        r#"This is a stub crate for cargo-nextest. Install nextest with:

    cargo install --locked cargo-nextest

Or see https://get.nexte.st for pre-built binaries."#
    );
}
